@@ not yet used. is this needed?
%<http://shex.io/extensions/javascript/js>:{
          const val = ShEx.ShExTerm.getLiteralValue(this.object);
          return isNaN(Date.parse(val))
            ? [{type: "SemActViolation", message: `"${val} is not a date`}]
            : []
        %}